name: BreedGenetics CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run tests
      run: python -m unittest test_genetics.py

  deploy:
    needs: backend-tests
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Debug Check Secrets
      run: |
        echo "Check if GCP_IP is defined..."
        if [ -z "${{ secrets.GCP_IP }}" ]; then echo "GCP_IP is EMPTY"; else echo "GCP_IP exists"; fi
        
    - name: Deploy to GCP VM via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.GCP_IP }}
        username: ${{ secrets.GCP_USERNAME }}
        key: ${{ secrets.GCP_SSH_KEY }}
        port: 22
        script: |
          if [ ! -d "PET_AI_ASSISTANT" ]; then
            git clone https://github.com/steve-shih/PET_AI_ASSISTANT.git
          fi
          cd PET_AI_ASSISTANT
          git fetch origin main
          git reset --hard origin/main
          docker compose down
          docker compose up -d --build